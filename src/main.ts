// src/main.ts
// App entry. Fleshed out at M7; placeholder keeps `dev` from erroring.

const app = document.querySelector<HTMLDivElement>('#app');
if (app) app.textContent = 'Diagram Evolver — build in progress.';

export {};
