// src/core/figure.ts
// Figure = Float64Array(48); seedToFigure; accessors; canonical index layout (M1)
// Stub — implemented in a later milestone (see plan / ARCHITECTURE.md §Module map).

export {};
