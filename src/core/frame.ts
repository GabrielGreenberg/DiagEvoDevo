// src/core/frame.ts
// Page (direction, no origin) + PositedFrame (origin+direction), fixed in v1 (M1)
// Stub — implemented in a later milestone (see plan / ARCHITECTURE.md §Module map).

export {};
